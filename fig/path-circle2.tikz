\begin{tikzpicture}
  \begin{axis}[domain=0:2*pi,
    samples=200,
    xmin=0.,
    axis equal image,
    axis lines=middle,
    legend pos = south west,
    xtick={pi,2*pi},
    xticklabels={$\pi$,$2\pi$},
    xlabel = $t$]
    \addplot[thick,blue]{cos(2*deg(x))};
    \addlegendentry{$x$}
    \addplot[thick,dashed,red]{sin(2*deg(x))};
    \addlegendentry{$y$}
  \end{axis}
\end{tikzpicture}
